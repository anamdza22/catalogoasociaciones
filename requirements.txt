with st.expander("Ver información completa"):
    st.markdown("### Requisitos específicos")

    requisitos = str(row["requisitos"]).split(";")

    for req in requisitos:
        st.write(f"• {req.strip()}")
